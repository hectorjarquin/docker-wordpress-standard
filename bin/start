#!/bin/bash
set -a
source .env

# Auto-generate SSL certificates if they don't exist
if [ ! -f "./config/nginx/ssl/${APP_DOMAIN}.pem" ]; then
  echo "SSL certificates not found. Generating..."
  bin/setup-ssl
fi

bin/fix-permissions
bin/fix-owner

docker-compose up "$@"
